# 家庭记账本

- 如果修改此项目里面的代码文件，那么请同步更新此 MD 文档。

<!-- USER-RULES:START -->
<!-- 用户自定义规则写在这里；如果这里除了本注释外没有任何内容，则忽略本区块。 -->
<!-- USER-RULES:END -->

## 代码简介

这是一个基于 Streamlit 的家庭记账小应用，当前实现了支出录入、支出列表查看、AA 支出结清计算，以及基于本地 SQLite 数据库的结清记录查询。

## 目录结构

```text
家庭记账本/
├── .claude/ # 本地 AI 工具相关配置目录，不属于应用运行逻辑。
├── .playwright-mcp/ # 本地浏览器自动化工具目录，用于开发时辅助调试。
├── .venv/ # 本地 Python 虚拟环境目录，用于隔离项目依赖。
├── .gitignore # Git 忽略规则，避免提交本地环境文件和运行时产物。
├── AGENTS.md # 项目级说明文档，记录仓库规则、代码简介和目录结构。
├── app.py # Streamlit 应用入口，负责页面切换、表单交互、列表展示和结清流程。
└── database/ # SQLite 数据访问层，封装建表、支出读写和结清逻辑。
    ├── CLAUDE.md # 现有目录级说明文档，描述数据库目录职责。
    └── __init__.py # 数据库初始化和所有支出、结清相关查询与写入逻辑。
```
